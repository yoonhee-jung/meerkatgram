/**
 * @file app/middlewares/auth/configs/provider/enum.js
 * @description 로그인제공자 설정 상수 설정
 * 251126 v1.0.0 yoonhee init
 */

const PROVIDER = {
      NONE: 'NONE',
      KAKAO: 'KAKAO',
};

Object.freeze(PROVIDER);

export default PROVIDER;